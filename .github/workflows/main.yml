name: Deploy to GitHub Pages

on:
  push:
    branches:
      - prod

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Build Project
        run: npm run build

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v5  :contentReference[oaicite:0]{index=0}

      - name: Upload to GitHub Pages
        uses: actions/upload-pages-artifact@v3  :contentReference[oaicite:1]{index=1}
        with:
          path: ./build  # Change if your build directory is different

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4  :contentReference[oaicite:2]{index=2}